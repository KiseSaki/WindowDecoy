namespace WindowDecoy.Models;

public enum WindowSizeMode
{
    TargetWindow,
    ScreenshotSize,
    Custom
}

public enum WindowPositionMode
{
    TargetWindow,
    ScreenCenter,
    Custom
}

public enum DecoyBehaviorMode
{
    Proxy = 0,
    Safe = 1
}